# Generated — reformatting a lockfile invites needless churn and merge conflicts.
pnpm-lock.yaml

# Prettier's markdown-ish escaping has corrupted the `*` globs in this file before
# (they became `_` and `\*`, silently disabling every wildcard rule).
.gitignore

# Generated by `pnpm generate` — Prisma 7 emits the client into the project.
src/generated/
